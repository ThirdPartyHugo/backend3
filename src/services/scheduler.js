const schedule = require('node-schedule');

const setupScheduler = () => {
  // Scheduler will be implemented with specific tasks
  console.log('Scheduler service initialized');
};

module.exports = setupScheduler;